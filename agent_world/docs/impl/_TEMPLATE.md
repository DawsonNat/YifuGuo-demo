# <模块名> 实现文档

> 路径: `<repo 内目标文件路径>`
> 对应 LAYOUT §: <e.g. §2.A WorldStep / §6.1 / §3.2>
> 上游依赖文档: <同目录其他 impl 文档名, 或"无">
> 下游依赖文档: <同目录其他 impl 文档名, 或"无">

## 1. 模块定位
一两段说清楚: 这个模块在系统里干嘛、为什么必须存在、它的"输入是什么、输出是什么"。

## 2. 借鉴来源
| 项 | 来源仓库 | 来源路径 (含行号) | 借鉴方式 | 备注 |
|---|---|---|---|---|

借鉴方式取值: `KEEP`(原样 import) / `EDIT`(直接改 fork) / `PATTERN`(仅作模式参考, 不引用) / `SKIP` / `NEW`(全新写)。

## 3. 关键改动 (相对来源仓库)
- 改动 1: …
- 改动 2: …

如果是全新写, 此节写"无来源, 全新写", 但仍需说明设计灵感来自哪里。

## 4. 核心逻辑

### 4.1 数据结构
in-memory 数据结构、字段约束、不变量。

### 4.2 关键流程 / 算法
按调用顺序写, 重点流程画箭头/伪代码即可, 不写真实实现。

### 4.3 与其他模块的交互
- 上游调用方: 谁会调本模块的什么 API
- 下游被调方: 本模块会调谁的什么 API
- 共享状态: 是否读/写 world.db、pool_*.db、Zep 哪些表/graph

## 5. 暴露 API

### 5.1 公开 class / function 签名
Python 伪签名 (含类型注解), 不写实现体:

```python
class XxxStore:
    def get(self, ...) -> ...: ...
    async def update(self, ...) -> None: ...
```

### 5.2 IPC / Flask / SQL (如适用)
- IPC 命令名 + 参数 schema (如适用)
- Flask 路由 (如适用)
- SQL 输入 / 输出表清单 (如适用)

## 6. 配置入口
从 `simulation_config.json` 哪些字段读取; 默认值; 验证规则。

## 7. 待决策 / 风险
LAYOUT §9 中尚未决议且影响本模块的项, 或本模块自己暴露的新风险。
